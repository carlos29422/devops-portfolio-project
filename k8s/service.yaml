apiVersion: v1
kind: Service
metadata:
  name: portfolio-api-service
  labels:
    app: portfolio-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: portfolio-api